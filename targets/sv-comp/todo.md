# 2026
## Errorneous Testcases
- [ ] java-ranger-regression/schedule_eqchk -> Recursion limit issue in binary execution tree
- [ ] The entire objects class, we need to handle the new `nondetObject()` call
- [x] autostub/Double_public_byte_java_lang_Double_byteValue -> Added proper 
- [ ] `jbmc-regression/StringCompare01` poses fundamental issue with deinterning.
## Unknown testcases
- [ ] `java-ranger-regression/nanoxml_eqchk/nanoxml_prop2`: This delegates from a pushback rteader to an IntReader which we now tolerate but the symbolic information oif the pushbvackreader is lost.
- [ ] `jbmc-regression/StringValueOf02` is missing constraint for IOOB for charAt. Can we add such constraints?
## Objects class
I think what we can at least do for `valid-assert` is to instrument the factory, then we can agian implicitly handle objects wihtout reasoning on them. 
But how can we do that given that its called inside the nondet that we dont handle?


## Todos
- [ ] Check and ensure that UFs are not caught as symbolic context loss naylonger due to implementation of UFs (`!fmgr.extractVariablesAndUFs(f).keySet().stream().allMatch(s -> s.matches("[A-Z].*_[0-9].*")))`)
- [ ] Can we add method for `Double/Float.toString()` ?
- [ ] Reconsider the delegation handling and unknown space tracking (`targets/sv-comp/sv-benchmarks/java/java-ranger-regression/nanoxml_eqchk/impl/DumpXML.java`)
- Figure out when the IASString appears and why
- [ ] Add Witness jar to swat verify? No i think the path needs to be fixed

### Runtime Exception Handling
- [ ] In normal operation when a RT exception is encoutnered operation should continue
- [ ] We need to properly catch runtime exceptions

## Fixes
- Symbolic explorer detects SWAT Exceptions (again)
- JVM narrowing conversion are now properly implemented
- Fixed interning issue for nonDet* functions

## Wrong testcases:
- `argv-tasks/AbstractSerializationStreamReader_true`
        `java -cp common:argv-tasks/AbstractSerializationStreamReader_true:. TestInputDirect`
        `cd /home/nils/dev/SWAT-Research/targets/sv-comp/sv-benchmarks/java && javac -cp common:argv-tasks/AbstractSerializationStreamReader_true TestInputDirect.java && java -ea -cp common:argv-tasks/AbstractSerializationStreamReader_true:. TestInputDirect`
- `argv-tasks/AlbersProjection_true` with all 0.0 causes assertion violation
    ```java
        public class Main {

        private static int N_ITER = 15;
        private static double EPSILON = -1.0e-7;
        private static double TOL = 1.0e-10;

        // protected double projectionLatitude1 = MapMath.degToRad(45.5);
        // protected double projectionLatitude2 = MapMath.degToRad(29.5);

        /** ARG-V: suitable */
        private static double phi1_(double qs, double Te, double Tone_es) {
            double sinpi = 0.0;
            double cospi = 0.0;
            double con = 0.0;
            double com = 0.0;
            double dphi = 0.0;

            double Phi = Math.asin(.5 * qs);
            if (Te < -(EPSILON)) return Phi;
            int i = N_ITER;
            do {
            sinpi = Math.sin(Phi);
            cospi = Math.cos(Phi);
            con = Te * sinpi;
            com = 1. - con * con;
            dphi =
                .5
                    * com
                    * com
                    / cospi
                    * (qs / Tone_es - sinpi / com + .5 / Te * Math.log((1. - con) / (1. + con)));
            Phi += dphi;
            } while (Math.abs(dphi) > TOL && --i != 0);
            return (i != 0 ? Math.abs(Phi) : Double.MAX_VALUE);
        }

        /** This main was generated by ARG-V */
        public static void main(String[] args) throws Exception {
            double result =
                phi1_(0.0, 0.0, 0.0);
                System.out.println(result);
            assert result > 0;
        }
        }
    ```
- `argv-tasks/HotspotIntegerGenerator_true` does not consider NaN causing assertion violation
    ```java
    public class Main {

  public int lowerBound = 0;
  public int upperBound = 0;
  public int hotInterval = 0;
  public int coldInterval = 0;
  public double hotsetFraction = 0.0;
  public double hotOpnFraction = 0.0;

  /**
   * Create a generator for Hotspot distributions.
   *
   * @param lowerBound lower bound of the distribution.
   * @param upperBound upper bound of the distribution.
   * @param hotsetFraction percentage of data item
   * @param hotOpnFraction percentage of operations accessing the hot set.
   */
  /** ARG-V: suitable */
  public Main(int lowerBound, int upperBound, double hotsetFraction, double hotOpnFraction) {
    if (hotsetFraction < 0.0 || hotsetFraction > 1.0) {
      System.err.println("Hotset fraction out of range. Setting to 0.0");
      hotsetFraction = 0.0;
    }
    if (hotOpnFraction < 0.0 || hotOpnFraction > 1.0) {
      System.err.println("Hot operation fraction out of range. Setting to 0.0");
      hotOpnFraction = 0.0;
    }
    if (lowerBound > upperBound) {
      System.err.println(
          "Upper bound of Hotspot generator smaller than the lower bound. "
              + "Swapping the values.");
      int temp = lowerBound;
      lowerBound = upperBound;
      upperBound = temp;
    }
    this.lowerBound = lowerBound;
    this.upperBound = upperBound;
    this.hotsetFraction = hotsetFraction;
    int interval = upperBound - lowerBound + 1;
    this.hotInterval = (int) (interval * hotsetFraction);
    this.coldInterval = interval - hotInterval;
    this.hotOpnFraction = hotOpnFraction;
  }

  /** This main was generated by ARG-V */
  public static void main(String[] args) throws Exception {
    Main instance =
        new Main(
            0,
            0,
            0.0,
            Double.NaN);
    System.out.println(instance.hotOpnFraction >= 0.0 && instance.hotOpnFraction <= 1.0);
    assert instance.hotOpnFraction >= 0.0 && instance.hotOpnFraction <= 1.0;
    assert instance.upperBound >= instance.lowerBound;
    assert instance.hotsetFraction >= 0.0 && instance.hotsetFraction <= 1.0;
  }
}
```
- `argv-tasks/SpdyStream` can also be violated despite true label
```java
public class Main {
  public static int read(byte[] b, int count) throws Exception {
    int windowUpdateThreshold = 0;
    int unacknowledgedBytes = 0;
    int limit = 1073741824;

    if (limit >= 0) {
      int pos = 1073741824;
      if (pos == -1) {
        return -1;
      }
      if (pos >= 0) {
        assert (pos >= 0);
        int copied = 0;
        int bytesToCopy = 1073741824;
        if (bytesToCopy >= 0) {
          //         drain from [pos..buffer.length)
          if (limit <= pos) {
            pos += bytesToCopy;
            copied += bytesToCopy;
            if (pos == -2147483648) {
              pos = 0;
            }
          }
          // drain from [pos..limit)
          if (copied < count) {
            pos += bytesToCopy;
            copied += bytesToCopy;
          }
        }
        if (pos == limit) {
          pos = -1;
          limit = 0;
        }
        System.out.println(copied >= 0);
        assert (copied >= 0); // make note in document
        return copied;
      } else {
        return -1;
      }
    }
    return -1;
  }

  public static void main(String[] args) {
    int n = 0;
    if (n >= 0) {
      byte[] b = new byte[n];
      int count = 1073741825;
      if (count > 0) {
        try {
          int copies = read(b, count);

        } catch (Exception e) {
          System.out.println(e);
        }
      }
    }
  }
}
```
- `argv-tasks/Constant_true` this case also needs to be checked
- `argv-tasks/FractalTouchHandler_true` same here
- `argv-tasks/MathHelper_true` +1
- `argv-tasks/PieSegment_true` here NaN seems to be the issue again
- `argv-tasks/Piecewise_true` here nan is also likely the issue
- `argv-tasks/SunTimes_true` +1 NaN
- `argv-tasks/ReverseInterpolator_true`:
```java
    public class Main {

    /** ARG-V: suitable */
    public float getInterpolation(float t) {
        float x = t * 2.0f;
        if (t < 0.5f) return 0.5f * x * x * x * x * x;
        return 0.5f * x * x * x * x * x + 1;
    }

    /** This main was generated by ARG-V */
    public static void main(String[] args) throws Exception {
        Main instance = new Main();
        float input = 16.021587f;
        float result = instance.getInterpolation(input);
        System.out.println(Math.abs(input * input * input * input * input * 16.0f - result) < 1.1f);
        assert Math.abs(input * input * input * input * input * 16.0f - result) < 1.1f;
    }
  ```
# 2025
- Make Symbolic executur logdir configurable:
    Away from using cwd: Saving to: {cwd}/logs/instrumented/    
- Add loggers for cosntraints, coverage, sanity checks etc...
- Reset Adresses to -1 in all function invocation models where a new i.e. String is returned, else it will run into assertion issue
- [ ] untangle execution status and verdict...
- [ ] Think about timeout for Java execution vs timeout for entire testcase, maybe small javatimeout allows testcase to run based on other found solution?
- [ ] Proper implementation of verifier.assume
- [ ] Find and fix issue wiht loop unrolling
- [ ] [SWAT] Uncaught top-level exception in symbolic execution -> Retry in this case maybe?
- [ ] Investigate recursion limit crash
- [ ] Find a proper way to implement hasModel checkk for invocations
- [ ] Evaluate proper way to aassign nonDet IDs for varying control flow and occurrence patterns
- [ ] Proper checking in Util.isMatchingType for implicit casts




Error cases:
- "securibench\\Basic38" -> Missing nextToken/ hasmoreelements
- "float-nonlinear-calculation\\coral63" -> Missing MAth.sqrt among other Math functions
- "java-ranger-regression\\apachecli_eqchk" investigate timeout
- "securibench\\Basic26" Some missing methods
- "securibench\\Collections7" Some context loss 
- ""java-ranger-regression\\siena_eqchk\\siena_prop2" investigate timeout 
- "securibench\\Inter7" Discuss with Florian -> Broken invocation stack
- securibench\\Collections6 Discuss with Florian -> Map implementation Value as key does not work, what else?


Wrong testcases
"float-nonlinear-calculation\\coral79"    -> Wrong label (?) I can find values that cause assertion violation
Arrays10 -> Can never cause violation, due to crash in array size